// stdio.h is a library we need to use (in scratch this was like the camera, audio, etc.)
#include <stdio.h>

// main is similar to "when green flag clicked" in scratch
int main(void)
{
    // printf is similar to "say _____" in scratch
    printf("hello world! \n");
}